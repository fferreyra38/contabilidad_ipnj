{% extends 'base.html.twig' %}

{% block body %}
    <h1>Tesorero list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for tesorero in tesoreros %}
            <tr>
                <td><a href="{{ path('tesorero_show', { 'id': tesorero.id }) }}">{{ tesorero.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('tesorero_show', { 'id': tesorero.id }) }}">show</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    
{% endblock %}
